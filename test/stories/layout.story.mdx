import {
  Canvas,
  Meta,
  Story,
  Description,
  ArgsTable,
} from '@storybook/addon-docs'
import { parameters, args } from '../../.storybook/preview'
import {
  aDocSpace,
  aDocBlock,
  aLayoutBlock,
  aLayout,
} from './helpers/documentation'

<Meta
  title="Atoms/Layouts"
  argTypes={{
    layoutName: {
      options: [
        'full',
        'center',
        'focus',
        'halves',
        'thirds',
        'quarters',
        'right',
        'left',
      ],
      control: { type: 'select' },
      ...args('Property', 'full')
    },
    layoutMargin: {
      options: ['full', 'single', 'none'],
      control: { type: 'select' },
      ...args('Property', 'full')
    },
    children: args('Parameter', 'undefined'),
  }}
/>

<!-- -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  -  - -->

<!-- prettier-ignore-start -->

export const Layout = ({layoutName, layoutMargin}) =>
  aDocSpace(
    aLayout.aLayout(
      { layoutName: layoutName, layoutMargin: layoutMargin },
      aLayoutBlock.aLayoutBlock({ subgrid: true }, ...aDocBlock(12)),
      aLayoutBlock.aLayoutBlock({ subgrid: true }, ...aDocBlock(12)),
      aLayoutBlock.aLayoutBlock({ subgrid: true }, ...aDocBlock(12)),
      aLayoutBlock.aLayoutBlock({ subgrid: true }, ...aDocBlock(12))
    )
  )

<Description>{aLayout.doc.aLayout}</Description>

<!-- prettier-ignore-end -->

## Live Edit

<Story
  name="Layout"
  args={{ layoutMargin: 'full', layoutName: 'full' }}
  parameters={{
    controls: {
      include: [
        'layoutName',
        'layoutMargin'
      ],
    },
  }}
>
  {Layout.bind({})}
</Story>

<ArgsTable story="Layout" />
